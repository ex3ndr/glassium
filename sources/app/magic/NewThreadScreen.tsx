import * as React from 'react';
import { View } from 'react-native';
import { Theme } from '../../theme';

export const NewThreadScreen = React.memo(() => {
    return (
        <View style={{ flexGrow: 1, backgroundColor: Theme.background }}>

        </View>
    );
});